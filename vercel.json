{
  "version": 2,
  "builds": [
    {
      "src": "server.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    { "src": "/project/[^/]+", "dest": "/server.js" },
    { "handle": "filesystem" },
    { "src": "/.*", "dest": "/server.js" }
  ]
}
